{
  "name": "hass-oidc-provider",
  "version": "1.0.0",
  "description": "Use Home Assistant as a standard OpenID Connect identity provider.",
  "license": "MIT",
  "author": "Adam Jones (domdomegg)",
  "repository": {
    "type": "git",
    "url": "https://github.com/domdomegg/hass-oidc-provider.git"
  },
  "bin": {
    "hass-oidc-provider": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "lint": "eslint",
    "clean": "rm -rf dist",
    "build": "tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^22.0.2",
    "@types/express": "^5.0.6",
    "eslint": "^9.32.0",
    "eslint-config-domdomegg": "^2.0.9",
    "tsconfig-domdomegg": "^1.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
